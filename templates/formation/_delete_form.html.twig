<form method="post" action="{{ path('app_formations_delete', {'id': formations.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formations.id) }}">
    <button class="btn">Delete</button>
</form>